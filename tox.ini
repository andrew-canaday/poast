[tox]
;envlist = py37, py38, flake8
envlist = py37, py38

[travis]
python =
    3.8: py38
    3.7: py37
;    3.6: py36
;    3.5: py35

[testenv:flake8]
basepython = python
deps = flake8
;commands = flake8 poast tests
commands = echo "Initial setup. Skip flake8"

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements_dev.txt
     -r{toxinidir}/requirements.txt
commands =
    pip install -U pip
    pytest --basetemp={envtmpdir}

